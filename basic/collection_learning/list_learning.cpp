#include <list>
#include <random>
#include <cmath>
#include <math.h>



int main() {
    std::list<int> listDemo;
    listDemo.push_back(1);

    for (int i = 0;i < 10;i++) {
        
    }

    std::list<int> listDemo2(listDemo);
    return 0;
}